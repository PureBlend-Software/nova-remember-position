# Changelog

## 1.0

Initial release. Reopens each document scrolled to the cursor position it had the
last time it was open. Positions are tracked as you work, saved on stop-typing /
save / close / quit, and stored per document so they persist across restarts and
windows. Includes a "Clear Saved Positions" command.
